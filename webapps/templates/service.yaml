apiVersion: v1
kind: Service
metadata:
  name: {{ include "webapps.fullname" . }}
  labels:
    {{- include "webapps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webapps.selectorLabels" . | nindent 4 }}
